{
  "name": "visualizeRevTree",
  "version": "0.0.1",
  "repository": "https://github.com/neojski/visualizeRevTree",
  "dependencies": {
    "pouchdb": "^3.4.0"
  },
  "scripts": {
    "build": "browserify --debug index.js > dist/index.js",
    "watch": "watchify index.js --debug -v -o dist/index.js"
  }
}
